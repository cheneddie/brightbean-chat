# Production environment for docker-compose.prod.yml.
#
#   cp deploy/env.prod.example .env      # compose reads ./.env for interpolation
#   make prod-secrets                    # generates the four values below
#   $EDITOR .env
#
# Every value in the REQUIRED block is empty on purpose. `docker compose -f
# docker-compose.prod.yml up` refuses to start while any of them is unset and
# names the one it is missing — a wrong-but-present placeholder would boot, and
# a deployment signing sessions with a string published in this repository is
# the failure this file exists to prevent (SECURITY-BASELINE §8).
#
# Full walkthrough: docs/self-hosting.md. Every optional variable this app
# understands is documented in .env.example at the repo root; this file carries
# the subset a production deployment actually decides.

# ---------------------------------------------------------------------------
# REQUIRED — the stack will not start until all five are set
# ---------------------------------------------------------------------------

# The one hostname this deployment answers on, without a scheme. It configures
# Caddy's certificate, Django's ALLOWED_HOSTS and the APP_URL that public links
# (unsubscribe, click tracking, media delivery) are built from. Its DNS record
# must already point at this host before the first `up`, or the certificate
# cannot be issued.
APP_DOMAIN=

# Where Let's Encrypt sends expiry and problem notices for that certificate.
ACME_EMAIL=

# Django's signing key, and the input to credential encryption.
SECRET_KEY=

# The HKDF salt for the AES-256-GCM encrypted fields. A DIFFERENT random value
# from SECRET_KEY.
#
# Back both of these up somewhere other than the database. A dump contains the
# encrypted platform credentials; these two are what decrypt them. Lose them and
# every stored credential is unreadable — see docs/self-hosting.md § Backups.
ENCRYPTION_KEY_SALT=

# Planned encryption-key rotation only. Each entry is the PREVIOUS paired
# SECRET_KEY + ENCRYPTION_KEY_SALT generation. New writes always use the
# primary values above; reads and HMAC lookups may temporarily fall back.
# Keep this on one line in .env. Remove only after the rotation runbook says
# every required old credential/link has been migrated, expired, reissued or
# intentionally invalidated.
# ENCRYPTION_KEY_FALLBACKS=[{"secret_key":"old-secret","salt":"old-salt"}]

# The password for the bundled Postgres. It ends up inside a URL, so keep it
# URL-safe — `make prod-secrets` generates one that is.
POSTGRES_PASSWORD=

# ---------------------------------------------------------------------------
# OPTIONAL
# ---------------------------------------------------------------------------

# Only when this deployment answers on more than one name, or when APP_URL must
# differ from https://$APP_DOMAIN. Both default to APP_DOMAIN.
# ALLOWED_HOSTS=chat.example.com,www.example.com
# APP_URL=https://chat.example.com

# The bundled Postgres. Change these before the FIRST start — afterwards the
# database is already initialised with the old values and changing them here
# only breaks the connection string.
# POSTGRES_DB=brightbean_chat
# POSTGRES_USER=brightbean

# Point the app at a managed Postgres instead of the bundled one. Setting this
# wins over the URL assembled from POSTGRES_*.
# DATABASE_URL=

# Where uploaded media and queued contact-import files live: "local" (the
# media_data volume, which the app and the worker both mount) or "s3".
#
# "local" is correct for THIS stack and wrong for every PaaS, where the web and
# worker processes share no filesystem — see docs/self-hosting.md § Storage. The
# S3_* names are generic, so AWS S3, Cloudflare R2, Backblaze B2 and MinIO all
# work with the same five. Keep the bucket private; delivery URLs are signed.
# STORAGE_BACKEND=local
# S3_BUCKET_NAME=
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_ENDPOINT_URL=            # blank for AWS; set it for R2, B2 or MinIO
# S3_REGION_NAME=auto

# Which image the stack runs. This project publishes none, so IMAGE_TAG names
# what `docker compose build` produces locally and `pull` has nothing to fetch.
# Point IMAGE_REPOSITORY at a registry if you run a fork that does publish one,
# and `pull` starts working — see docs/self-hosting.md § Upgrades.
# IMAGE_REPOSITORY=brightbean-chat
# IMAGE_TAG=latest

# For a host that already has something on 80/443 and forwards to these. Note
# that automatic HTTPS needs port 80 reachable from the internet for the ACME
# challenge, so changing these means terminating TLS elsewhere — see
# deploy/docker-compose.external-tls.yml.
# CADDY_HTTP_PORT=80
# CADDY_HTTPS_PORT=443

# Where the app is published when you terminate TLS elsewhere
# (deploy/docker-compose.external-tls.yml). Bound to 127.0.0.1 either way: only
# a proxy on this host may reach it.
# APP_BIND_PORT=8000

# Which peers may speak for someone else via X-Forwarded-For. Defaults to
# Docker's own bridge range, which is Caddy — the only thing that can reach the
# app. Widen it only if a second proxy sits in front of Caddy, and only to that
# proxy's address; the header is attacker-controlled otherwise and trusting it
# turns the auth rate limiting off.
# TRUSTED_PROXIES=172.16.0.0/12

# The shared secret for /internal/tick, an HTTP wrapper around one worker cycle.
# The compose stack runs a real worker, so leave this EMPTY unless you also want
# a cron service or uptime pinger to be able to drain the queue. The route 404s
# while it is unset. Treat it as a credential.
# TICK_TOKEN=

# /internal/queue-status marks a due queue item as degraded after this many
# seconds. The endpoint is protected by TICK_TOKEN and never mutates the queue.
# Stale running work is a hard error regardless of this threshold.
# QUEUE_STATUS_OVERDUE_WARN_SECONDS=60
# Worker crash/zombie recovery threshold. A legitimate single handler must not
# run longer than this without checkpointing updated_at.
# QUEUE_ZOMBIE_AFTER_SECONDS=600
# Maximum age of the latest worker/CLI tick/HTTP tick heartbeat before queue
# status becomes HTTP 503. If tick mode runs less often than this, raise it to
# comfortably exceed the scheduler interval.
# QUEUE_CONSUMER_HEARTBEAT_MAX_AGE_SECONDS=120
# Maximum active (pending + running) queued actions one workspace may hold.
# Terminal history does not count. Set <= 0 only if you deliberately want an
# unbounded tenant backlog.
# QUEUE_MAX_ACTIVE_PER_WORKSPACE=10000

# Broader incident thresholds used by `python manage.py ops_snapshot`.
# A webhook still in "received" beyond this age is a hard operational error.
# OPS_WEBHOOK_STUCK_SECONDS=300
# Recent webhook failures become degraded once this many occur inside the
# configured rolling window.
# OPS_WEBHOOK_FAILURE_WINDOW_SECONDS=900
# OPS_WEBHOOK_FAILURE_WARN_COUNT=5

# Platform app credentials — where the Meta app credentials are set, plus the
# token Meta checks when you subscribe a webhook URL. There is no tenant-facing
# UI for these; an organization row in the Django admin (superuser only) is the
# fallback for the id/secret of a deployment serving several organizations, and
# anything set here outranks it. The VERIFY_TOKEN values are always read from
# here — the verification GET names no organization, so the admin cannot supply
# one. See .env.example for the full list and docs/channels/ for where each
# value comes from.
# PLATFORM_INSTAGRAM_CLIENT_ID=
# PLATFORM_INSTAGRAM_CLIENT_SECRET=
# PLATFORM_INSTAGRAM_VERIFY_TOKEN=
# PLATFORM_MESSENGER_CLIENT_ID=
# PLATFORM_MESSENGER_CLIENT_SECRET=
# PLATFORM_MESSENGER_VERIFY_TOKEN=
# PLATFORM_WHATSAPP_CLIENT_ID=
# PLATFORM_WHATSAPP_CLIENT_SECRET=
# PLATFORM_WHATSAPP_VERIFY_TOKEN=

# SMTP, for password reset and email verification. Without it those messages
# cannot be delivered; nothing else is gated on them.
# EMAIL_HOST=
# EMAIL_PORT=587
# EMAIL_HOST_USER=
# EMAIL_HOST_PASSWORD=
# EMAIL_USE_TLS=true
# DEFAULT_FROM_EMAIL=noreply@example.com

# Error reporting. Empty disables Sentry entirely.
# SENTRY_DSN=

# DEBUG is deliberately absent. config/settings/production.py forces it off
# before it loads anything else, so a stray DEBUG=true here changes nothing —
# which is the point.
